# .github/workflows/hello.yml

name: Hello World

on:
  push:
  workflow_dispatch:

jobs:
  say-hello:
    name: 打印问候语
    runs-on: ubuntu-latest

    steps:
      - name: 打印消息
        run: echo "Hello, GitHub Actions!"